<nav class="navbar navbar-default row">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <span class="navbar-brand">
            <img src="{{ asset('images/logo_32x32.png') }}"/>
            <a href="{{ path('homepage') }}">La Bouée Corsaire</a>
        </span>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="btn btn-primary btn-lg" href="{{ path('admin_home') }}">
                            Panneau d’administration
                        </a>
                    {% else %}
                        <a class="btn btn-primary btn-lg dropdown-toggle" data-toggle="dropdown" role="button" arial-expanded="false" href="{{ path('fos_user_profile_show') }}">
                            {{ app.user.name }}
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <a class="btn btn-lg" href="{{ path('fos_user_profile_show') }}">Mon profil</a>
                            <a class="btn btn-lg" href="{{ path('task_list_owned') }}">Mes besoins</a>
                            <a class="btn btn-lg" href="{{ path('transaction_list') }}">Mes transactions</a>
                            <a href="{{ logout_path() }}" data-original-title="Deconnexion" data-toggle="tooltip" type="button" class="btn btn-lg">
                                <i class="glyphicon glyphicon-log-out"></i>
                                Déconnexion
                            </a>
                        </ul>
                    {% endif %}
                {% else %}
                    <a class="btn btn-primary btn-lg" href="{{ path('user_login') }}">
                        Connexion/Inscription
                    </a>
                {% endif %}
            </li>
            <li>
                <form class="navbar-form navbar-left"  action="{{ path('search_task_nav') }} " method="post">
                    <div class="input-group">
                        <input type="search" class="form-control" placeholder="Rechercher" name="search">
                        <div class="input-group-btn">
                            <button class="btn btn-default" type="submit"">
                                <i class="glyphicon glyphicon-search"></i>
                            </button>
                        </div>
                    </div>
				</form>
                </li>
                <li>
                    <!--Facebook-->
                    <a href="#">
                        <i class="fa fa-facebook-official fa-2x" aria-hidden="true"></i>
                    </a>
                </li>
                <li>
                    <!--Twitter-->
                    <a href="#">
                        <i class="fa fa-twitter fa-2x" aria-hidden="true"></i>
                    </a>
                </li>
                <li>
                    <!--RSS-->
                    <a href="#">
                        <i class="fa fa-rss-square fa-2x" aria-hidden="true"></i>
                    </a>
                </li>
            </ul>
        </div>
    </nav>
